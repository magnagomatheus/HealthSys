#include <stdio.h>
#include <stdlib.h>

#include "paciente.h"
#include "menu.h"
//#include <locale.h>

int main() {

    //LinkedList *pacientes = criar_ListaPacientes();
    //setlocale(LC_ALL, "pt_BR.utf8");
    menu();
    //free(pacientes);
    return 0;
}